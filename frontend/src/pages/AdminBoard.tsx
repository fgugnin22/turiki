import React from "react";
import { Layout } from "../widgets/Layout";

const AdminBoard = () => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
};

export default AdminBoard;
