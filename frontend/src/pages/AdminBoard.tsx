import React from "react";
import { Layout } from "../processes/Layout";

const AdminBoard = () => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
};

export default AdminBoard;
